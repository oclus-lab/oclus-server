# Oclus Common

This crate serves as a shared library for re-exporting common structs or types that can be used by the client.
By centralizing common code into this crate, you can easily maintain consistency and reduce duplication in your
projects.

## Features

- Contains definitions for common structs or types.
- Ensures type consistency between client and server implementations.
- Lightweight and easy to integrate into your project.
